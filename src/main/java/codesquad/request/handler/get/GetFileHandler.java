package codesquad.request.handler.get;

import codesquad.request.format.ClientRequest;
import codesquad.request.handler.RequestHandler;

public class GetFileHandler implements RequestHandler {
    @Override
    public String handler(ClientRequest request) {

        return "";
    }
}
